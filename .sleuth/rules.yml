rules:
  - my-friggin-rule:
      conditions:
        - environment='Production'
      actions:
        - trigger_build:
            parameters:
              name: 'blah.yml'
              promote_and_deploy_tag: true
              environment: Production
              version: "{{ revision }}"
