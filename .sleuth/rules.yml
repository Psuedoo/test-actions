rules:
  - slack-notif:
      conditions:
        - environment='Production'
      actions:
        - slack_channel_message:
            channel: "#psu-test-channel"
            message: This is a test
        - add_deploy_tag: "lolol"
