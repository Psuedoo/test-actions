rules:
  - slack-notif:
      conditions:
        - environment='production'
      actions:
        - slack_channel_message:
            channel: "#psu-test-channel"
            message: |
              {{revision}}
