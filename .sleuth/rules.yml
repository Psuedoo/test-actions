rules:
  - slack-notif:
      conditions:
        - environment='Production'
      actions:
        - trigger_build:
            parameters:
              name: 'blank.yml'
              promote_and_deploy_tag: true
              environment: Production
              version: "{{ revision }}"
